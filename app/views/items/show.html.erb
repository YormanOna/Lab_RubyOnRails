<div class="contenedor">
    <h1><%= @item.name %></h1>
    <p class="descripcion">Descripcion: <%= @item.description %></p>
    <p class="cantidad">Cantidad: <%= @item.quantity %></p>

    <div class="acciones">
      <%= link_to 'Editar', edit_item_path(@item), class: 'action-link' %>
      <%= link_to 'Regresar', items_path, class: 'action-link' %>
    </div>
  </div>